import React, { useState, useRef, useEffect } from "react";
import {
  View,
  TextInput,
  Text,
  Alert,
  ScrollView,
  Dimensions,
  StyleSheet
} from "react-native";

const VideoCard=props=>{
    <View>
            <Text>
                {props.title}
            </Text>
        </View>
}

export default VideoCard;